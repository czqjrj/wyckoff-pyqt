# 依赖以 pyproject.toml 为唯一来源, 本文件仅作可编辑安装入口。
# 可选组件按需使用 extras:
#   pip install ".[tts]"    # TTS 语音播报 (微软在线 + 离线)
#   pip install ".[test]"   # 测试 (pytest + pytest-cov)
#   pip install ".[lint]"   # 代码检查 (ruff)
#   pip install ".[qlib]"   # QLib 机器学习模型 (需自行编译环境)
-e .[tts]