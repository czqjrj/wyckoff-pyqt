"""UI 图表/控件集中常量 — 消除各 widget 文件散落的魔法数字。

按功能域分组; 修改常量只需改这一处, 全应用生效。
注意: TICKER_* 状态栏头条常量已集中在 wyckoff.config, 不在此重复。
"""

# ── K线图 ──────────────────────────────────────────────
# 默认视野显示的K线根数。K线图实际绘制宽度约 800~1000px (被自选股/分析
# 面板瓜分), 满幅(近3年≈700根)每根不足2px、250根也只有~4px, 实体不可辨,
# 看起来只剩均线与色带。默认聚焦最近 N 根(≈12px/根)让蜡烛清晰可见,
# 与主流看盘软件默认缩放一致 (Home/双击可看全幅)。
KLINE_DEFAULT_BARS = 80

# 最新K线右侧留白 (数据单位=根): 观察延伸/突破空间, 主流软件默认行为。
# (比例, 最小根数, 最大根数) — 按全幅根数的百分比取值并夹在区间内。
KLINE_RIGHT_MARGIN = (0.04, 6, 30)


# ── P&F 点数图 ─────────────────────────────────────────
# 默认视野显示的列数 (点数图通常列数较多, 满幅每列仅 2~4px, 格子与标注不可辨,
# 与主流看盘软件一致默认聚焦最近 N 列; Home/双击可看全幅)。
PNF_DEFAULT_COLS = 70

# 右缘留白 (列数): 放置 "上涨/下跌目标位" 标注
PNF_RIGHT_MARGIN = 9.0

# 左侧价格轴固定宽度 (px): 主图与底部量柱面板共用, 保证两视图 X 像素映射一致,
# 使量柱与点数图列严格对齐。
PNF_PRICE_AXIS_W = 64


# ── 技术指标 ───────────────────────────────────────────
# 与原版 matplotlib figsize=(8.5, 13.5) 的宽高比一致: 宽度铺满可视区, 高度按此比例。
IND_ASPECT = 13.5 / 8.5
# 资金透视整图纵横比 (高/宽): 与技术指标页同款 "宽度铺满、高度按比例、
# 竖向滚动" 容器 — 面板长宽比例不随窗口形状被压扁。
MKT_ASPECT = 14.5 / 10.0 * 2 / 3

# 技术指标 / 资金透视默认视野柱数 (0 = 全幅)。与 K线/P&F 一致默认聚焦最近 N
# 根让图形可读 (满幅≈700根每根仅~2px); Home/复位视图/双击 可看全幅。
# 由设置 Chart.IND_DEFAULT_BARS / MKT_DEFAULT_BARS 覆盖。
IND_DEFAULT_BARS = 250
MKT_DEFAULT_BARS = 120
